/**
 * 자산 수정 권한 구분.
 *
 * 서버가 엔드포인트로 갈라 놓았고, 화면도 그 경계를 따른다.
 *
 *   PATCH /asset/{id}             자산명·상태·매입처·부서·위치·담당자·모델명·규격·
 *                                 설비코드·계측기번호·출력제외·양도폐기일·비고
 *   PATCH /asset/{id}/correction  취득일자·취득가액·계정과목·내용연수·상각률·상각방법·
 *                                 개시 잔액 — reason 필수, 감가상각 전 기간 재계산
 *
 * 어느 필드가 어디에 속하는지는 각 API 모듈의 UpdateAssetPayload /
 * CorrectAssetPayload 타입이 곧 명세다. 여기에 목록을 또 두면 두 벌이 어긋난다.
 */

export const LOCKED_NOTICE =
  '정정 권한이 필요한 항목입니다. 값을 바꾸면 감가상각이 전 기간 재계산되며, 변경 이력이 남습니다.';
